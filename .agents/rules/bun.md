# Bun Rules

Always use `bun` as the package manager and runtime for this project.

- Install packages: `bun add <pkg>` or `bun add -d <pkg>`
- Run scripts: `bun run <script>`
- Run binaries: `bunx <binary>`
- Do not use `npm`, `yarn`, `pnpm`, or `npx`.
